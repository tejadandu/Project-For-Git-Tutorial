New demo repo
# Important git commands

### Begineer level commands

`git init` ---> it initialize a new repository 
- `git add <filename>` --> starts tracking your new changes for the next commit
- `git commit -m "<message>"` ---> 
- git internally does a lot of optimization the Objects are stored in compressed form
- it manualy stores data about the change & algorithimically shows
    the file content execute that change

- `git cat-file -t ` - executes the blob(object)
- `git cat-file -p` - print the content of the object

